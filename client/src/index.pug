html( lang="en" )
  head
    meta( charset="UTF-8" )
    meta( name="viewport" content="width=device-width, initial-scale=1.0" )

    link( rel="preload" href=require('@/assets/fonts/fira-code.woff2') as="font" type="font/woff2" crossorigin )
    link( rel="preload" href=require('@/assets/fonts/manrope.woff2') as="font" type="font/woff2" crossorigin )
    link( rel="preload" href=require('@/assets/fonts/manrope-bold.woff2') as="font" type="font/woff2" crossorigin )

    meta( name="theme-color" content=require('config').default.meta.themeColor )

    meta( property="og:url" content=require('config').default.meta.url )

    meta( name="twitter:image:src" content=require('config').default.meta.thumbnail )        
    meta( property="og:image" content=require('config').default.meta.thumbnail )
    meta( name="twitter:card" content="summary_large_image" )

    meta( property="og:type" content="object" )

    meta( property="og:description" name="description" content=require('config').default.meta.description )
    meta( name="twitter:description" content=require('config').default.meta.description )

    meta( name="twitter:site" content=require('config').default.meta.title )
    meta( property="og:title" content=require('config').default.meta.title )
    meta( name="twitter:title" content=require('config').default.meta.title )
    meta( property="og:site_name" content=require('config').default.meta.title )

    title #{ require('config').default.meta.title }

    style 
      //- | #{ require('@/css/preload.css') }
      | #{ require('@/css/prism.css') }
    
  body
    #app